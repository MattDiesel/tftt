
#ifndef TFTT_CONFIG_G
#define TFTT_CONFIG_G


#include <cstdint>


namespace tftt {

template<typename T, unsigned int DIM> struct TreeId;
class usermodel;
class tree;
class treecell;

typedef @TFTT_DATA_T@ data_t;
typedef @TFTT_MODEL_T@ model_t;
typedef TreeId<@TFTT_ID_DATA_T@,@TFTT_DIM@> ident_t;
typedef tree tree_t;
typedef treecell cell_t;

#define TFTT_DEBUG

enum {
	DIM = @TFTT_DIM@,
	MaxDepth = @TFTT_MAXDEPTH@,
	MinDepth = @TFTT_MINDEPTH@,
	BalanceMinChange = @TFTT_BALANCE_MINCHANGE@,
	BalanceCheckInterval = @TFTT_BALANCE_CHECKINTERVAL@
};


class usermodel {
public:
	static int cellIntensity(const data_t& d);
	static void constructCell(cell_t* cl);
	static void proc(cell_t* cl);
	static void postProc(tree_t& tr);
	static double flux(const data_t& a, const data_t& b);
};


} // namespace tftt


#endif
